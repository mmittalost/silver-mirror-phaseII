import { LocationGroupPipe } from './location-group.pipe';

describe('LocationGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
